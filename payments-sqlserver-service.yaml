apiVersion: v1
kind: Service
metadata:
  name: payments-sqlserver
  namespace: fcg
spec:
  selector:
    app: payments-sqlserver
  ports:
    - name: sqlserver
      protocol: TCP
      port: 1433
      targetPort: 1433
  type: ClusterIP